import { WijzigWachtwoordDTO } from './wijzig-wachtwoord-dto';

describe('WijzigWachtwoordDTO', () => {
  it('should create an instance', () => {
    expect(new WijzigWachtwoordDTO()).toBeTruthy();
  });
});
